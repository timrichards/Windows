<local:LocalUserControlBase x:Class="DoubleFile.WinDoubleFile_Duplicates"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DoubleFile"
        LocalTitle="Duplicate Files">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" SelectedItem="{Binding SelectedItem}" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File name" Binding="{Binding Filename}" Width="{Binding WidthFilename}"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="{Binding WidthPath}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Command="{Binding Icmd_GoTo}" Content="Go To" Margin="0,10,10,10" Grid.Row="1" Width="70" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
    </Grid>
</local:LocalUserControlBase>
