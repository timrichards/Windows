<local:LocalWindow_DoubleFile x:Class="DoubleFile.WinTreeList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DoubleFile"
        Title="Double File" Width="600" Height="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" Name="LV_Siblings" ItemsSource="{Binding Siblings}" SelectedItem="{Binding SelectedItem}">
            <GridViewColumn Header="Folder name" DisplayMemberBinding="{Binding Folder}" Width="{Binding WidthFolder}"/>
        </ListView>
        <ListView Grid.Column="1" Name="LV_Children" ItemsSource="{Binding Children}" SelectedItem="{Binding SelectedItem}">
            <GridViewColumn Header="Folder name" DisplayMemberBinding="{Binding Folder}" Width="{Binding WidthFolder}"/>
        </ListView>
    </Grid>
</local:LocalWindow_DoubleFile>
